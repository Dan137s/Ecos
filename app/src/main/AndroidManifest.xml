<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.claseipst51android2021cl.grupo4">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- Le otorge permisos de usuario -->
    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Internet -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- Almacenamiento -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" /> <!-- Grabación de audio -->
    <uses-permission android:name="android.hardware.location.gps" /> <!-- GPS -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/c"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ecos">
        <activity android:name=".Ecos.MainActivityConverteEco"></activity>
        <activity android:name=".Ecos.MainActivityRegistro" />
        <activity android:name=".Ecos.MainActivityLogin" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps.MapsActivity3"
            android:label="@string/title_activity_maps3" />
        <activity
            android:name=".Maps.MapsActivity2"
            android:label="@string/title_activity_maps2" />
        <activity
            android:name=".Maps.MapsActivity1"
            android:label="@string/title_activity_maps1" />
        <activity android:name=".Ecos.MainActivityGrabadora1" />
        <activity android:name=".Ecos.MainActivityResgistroUser" />
        <activity android:name=".Ecos.MainActivityInicio" />
        <activity android:name=".Ecos.MainActivityCreaEco" />
        <activity android:name=".Ecos.MainActivityRestablecerClave" />
        <activity android:name=".Ecos.MainActivityAdmin" />
        <activity android:name=".Ecos.MainActivityCompartirApp" />
        <activity android:name=".Ecos.MainActivityUbicacion" />
        <activity android:name=".Ecos.MainActivityListadoEco" />
        <activity android:name=".Ecos.MenuActivityInvitado" />
        <activity android:name=".Ecos.MenuActivity" />
        <activity android:name=".Ecos.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>